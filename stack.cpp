// stack.cpp - Stack is fully templated in stack.h.
// This translation unit exists so the Makefile has something to compile,
// but all logic lives in the header (required for templates).
#include "stack.h"
